# Что не нужно на сервере. Список для выкладки, а не для git:
# в репозитории тесты и документация остаются — они нужны при разработке
# и служат бэкапом, а боту для работы не требуются.
#
# Выкладка:
#   rsync -av --delete --exclude-from=deploy/rsync-exclude.txt ./ user@server:/opt/aida/
#
# Папку data/ на сервере не трогаем: там материалы преподавателей и база.

.git/
.gitignore
.gitattributes
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Тесты и образцы билетов — только для разработки
tests/
data/raw/

# Документация
*.md
!README.md

# Локальные настройки: на сервере свой .env с боевым токеном
.env

# Данные: пересобираются на месте, по сети не гоняем
data/
